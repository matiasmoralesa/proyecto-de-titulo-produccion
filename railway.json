{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python backend/manage.py migrate --settings=config.settings.base && python backend/manage.py collectstatic --noinput --settings=config.settings.base && gunicorn wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
