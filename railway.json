{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --settings=config.settings.base && python manage.py collectstatic --noinput --settings=config.settings.base && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120 --chdir=/app/backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
